pipeline {
    agent { label 'sandbox-buildserver' } // if you don't have other steps, 'any' agent works
    stages {
        stage('SCM checkout') {
            
            steps {
                echo 'Build Now'
            }
        }
        
        }
}
